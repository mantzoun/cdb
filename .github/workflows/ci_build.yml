name: CI Build

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4

    - name: build
      run: make all
